export interface CreateUserData {
    displayName: string;
    description: string;
}

export interface CreateEventData {
    title: string;
    description: string;
    startDate: string;
    endDate: string;
    latitude: string;
    longitude: string;
    organisationId: string;
}
